# homebrew-tap

Homebrew tap for [iris-dev](https://github.com/intersystems-community/iris-dev) — MCP server connecting AI assistants to InterSystems IRIS.

## Install

```bash
brew tap intersystems-community/tap
brew install iris-dev
```

## Upgrade

```bash
brew upgrade iris-dev
```
